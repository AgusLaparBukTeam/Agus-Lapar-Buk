import { PageHeader } from "@/components/ui/page-header";
export default function ReferenceDataPage() { return <div className="operations-page"><PageHeader title="Reference data" description="Use named datasets for countries, currencies, units, transport modes, and document types." /><section className="data-panel data-panel--wide"><div className="data-panel__header"><div><h2>Workspace reference set</h2><p>Only sources that have been configured for this workspace are shown.</p></div></div><div className="empty-state"><strong>No custom reference data</strong><span>GateGuard uses the baseline values built into the workspace until a reference source is configured.</span></div></section></div>; }
