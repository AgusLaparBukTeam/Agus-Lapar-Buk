"use client";

import { Dialog, DialogRoot } from "@cloudflare/kumo/components/dialog";
import { Table } from "@cloudflare/kumo/components/table";
import { PlusIcon as Plus, UsersThreeIcon as UsersThree } from "@phosphor-icons/react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { PageHeader } from "@/components/ui/page-header";
import { createUser, fetchUsers, updateUser } from "@/lib/api";

export default function PeoplePage() {
  const client = useQueryClient();
  const result = useQuery({ queryKey: ["users"], queryFn: fetchUsers });
  const [open, setOpen] = useState(false);
  const [form, setForm] = useState({ display_name: "", email: "", password: "", role: "operator" });
  const mutation = useMutation({ mutationFn: () => createUser(form), onSuccess: () => { setOpen(false); setForm({ display_name: "", email: "", password: "", role: "operator" }); client.invalidateQueries({ queryKey: ["users"] }); } });
  const users = result.data || [];
  const active = users.filter((item) => item.active);
  const operators = users.filter((item) => item.role === "operator");
  const reviewers = users.filter((item) => item.role === "supervisor");
  const admins = users.filter((item) => item.role === "admin");
  return <div className="operations-page"><PageHeader icon={UsersThree} title="People and access" description="Manage who can prepare, review, approve, and administer shipment decisions." actions={<Button icon={Plus} onClick={() => setOpen(true)}>Add person</Button>} /><section className="metric-grid metric-grid--four"><div className="metric-cell"><span>Active users</span><strong>{active.length}</strong><small>Can sign in now</small></div><div className="metric-cell"><span>Operators</span><strong>{operators.length}</strong><small>Prepare shipment work</small></div><div className="metric-cell"><span>Reviewers</span><strong>{reviewers.length}</strong><small>Review and decide</small></div><div className="metric-cell"><span>Administrators</span><strong>{admins.length}</strong><small>Manage workspace access</small></div></section><section className="data-panel data-panel--wide"><div className="data-panel__header"><div><h2>Workspace people</h2><p>Access changes are recorded in the Activity log.</p></div></div><div className="table-scroll"><Table><Table.Header sticky><Table.Row><Table.Head>Person</Table.Head><Table.Head>Role</Table.Head><Table.Head>Status</Table.Head><Table.Head>Last login</Table.Head><Table.Head>Action</Table.Head></Table.Row></Table.Header><Table.Body>{users.map((item) => <Table.Row key={item.id}><Table.Cell><strong>{item.display_name}</strong><small>{item.email}</small></Table.Cell><Table.Cell>{item.role === "supervisor" ? "Reviewer" : item.role === "admin" ? "Administrator" : "Operator"}</Table.Cell><Table.Cell>{item.active ? "Active" : "Disabled"}</Table.Cell><Table.Cell>{item.last_login_at ? new Date(item.last_login_at).toLocaleString("en-GB") : "Never"}</Table.Cell><Table.Cell><Button size="sm" variant="secondary" disabled={item.role === "admin" && admins.length === 1} onClick={() => updateUser(item.id, { active: !item.active }).then(() => client.invalidateQueries({ queryKey: ["users"] }))}>{item.active ? "Deactivate" : "Reactivate"}</Button></Table.Cell></Table.Row>)}</Table.Body></Table></div>{!users.length && <div className="empty-state"><strong>No people yet</strong><span>Add an operator, reviewer, or administrator to begin.</span></div>}</section><DialogRoot open={open} onOpenChange={setOpen}><Dialog className="person-dialog"><Dialog.Title>Add person</Dialog.Title><Dialog.Description>Create an account with a temporary password. The person must change it after first sign-in.</Dialog.Description><form className="dialog-form" onSubmit={(event) => { event.preventDefault(); mutation.mutate(); }}><label>Display name<input required value={form.display_name} onChange={(event) => setForm({ ...form, display_name: event.target.value })} /></label><label>Email<input required type="email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} /></label><label>Temporary password<input required minLength={12} type="password" value={form.password} onChange={(event) => setForm({ ...form, password: event.target.value })} /></label><label>Role<select value={form.role} onChange={(event) => setForm({ ...form, role: event.target.value })}><option value="operator">Operator</option><option value="supervisor">Reviewer</option><option value="admin">Administrator</option></select></label>{mutation.isError && <p role="alert" className="form-error">{(mutation.error as Error).message}</p>}<div className="form-panel__actions"><Button type="button" variant="secondary" onClick={() => setOpen(false)}>Cancel</Button><Button type="submit" disabled={mutation.isPending}>{mutation.isPending ? "Adding…" : "Add person"}</Button></div></form></Dialog></DialogRoot></div>;
}
