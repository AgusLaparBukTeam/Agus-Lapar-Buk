"use client";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { useState } from "react";
import { createUser, fetchUsers, updateUser } from "@/lib/api";
import type { UserRole } from "@/lib/types";
export default function UsersPage() { const client = useQueryClient(); const { data, isPending, isError } = useQuery({ queryKey: ["users"], queryFn: fetchUsers }); const [form, setForm] = useState({ email: "", display_name: "", password: "", role: "operator" as UserRole }); const mutation = useMutation({ mutationFn: () => createUser(form), onSuccess: () => { setForm({ email: "", display_name: "", password: "", role: "operator" }); client.invalidateQueries({ queryKey: ["users"] }); } }); const toggle = useMutation({ mutationFn: (value: { id: string; active: boolean }) => updateUser(value.id, { active: value.active }), onSuccess: () => client.invalidateQueries({ queryKey: ["users"] }) }); if (isPending) return <p className="py-12 text-sm text-[var(--subtle)]">Memuat users…</p>; if (isError || !data) return <div role="alert" className="rounded-md border border-red-200 bg-red-50 p-4 text-sm text-red-800">User management hanya tersedia untuk admin.</div>; return <div><p className="text-xs uppercase tracking-[0.16em] text-[var(--subtle)]">Administration</p><h1 className="mt-2 text-2xl font-semibold">User management</h1><p className="mt-1 text-sm text-[var(--subtle)]">Akun terpisah dengan role dan status yang dapat diaudit.</p><section className="mt-7 rounded-lg border border-[var(--border)] bg-white p-5"><h2 className="font-semibold">Create user</h2><form onSubmit={(e) => { e.preventDefault(); mutation.mutate(); }} className="mt-4 grid gap-3 md:grid-cols-2"><input required placeholder="Display name" value={form.display_name} onChange={(e) => setForm({ ...form, display_name: e.target.value })} className="h-10 rounded-md border border-[var(--border)] px-3 text-sm" /><input required type="email" placeholder="Email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} className="h-10 rounded-md border border-[var(--border)] px-3 text-sm" /><input required minLength={12} type="password" placeholder="Initial password (12+ chars)" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} className="h-10 rounded-md border border-[var(--border)] px-3 text-sm" /><select value={form.role} onChange={(e) => setForm({ ...form, role: e.target.value as UserRole })} className="h-10 rounded-md border border-[var(--border)] bg-white px-3 text-sm"><option value="operator">Operator</option><option value="supervisor">Supervisor</option><option value="admin">Admin</option></select><button disabled={mutation.isPending} className="h-10 rounded-md bg-[var(--accent)] px-4 text-sm font-medium text-white md:col-span-2 disabled:opacity-60">{mutation.isPending ? "Menyimpan…" : "Create account"}</button></form></section><section className="mt-5 overflow-x-auto rounded-lg border border-[var(--border)] bg-white"><table className="w-full min-w-[650px] text-left text-sm"><thead className="bg-[var(--muted)] text-xs text-[var(--subtle)]"><tr><th className="px-4 py-3">User</th><th className="px-4 py-3">Role</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Action</th></tr></thead><tbody>{data.map((user) => <tr key={user.id} className="border-t border-[var(--border)]"><td className="px-4 py-3"><div className="font-medium">{user.display_name}</div><div className="text-xs text-[var(--subtle)]">{user.email}</div></td><td className="px-4 py-3 capitalize">{user.role}</td><td className="px-4 py-3">{user.active ? "Active" : "Inactive"}</td><td className="px-4 py-3"><button onClick={() => toggle.mutate({ id: user.id, active: !user.active })} className="text-xs font-medium text-[var(--accent)]">{user.active ? "Deactivate" : "Activate"}</button></td></tr>)}</tbody></table></section></div>; }
